---
import Navbar from "./Navbar.astro";
import heroVideo from "../assets/kc-wine.mp4";
import logo from "../assets/vektor/kc-bw-logo.svg";
---

<header class="hero-header">
  <nav class="navbar">
    <Navbar />
  </nav>

  <div class="hero-video-container">
    <video class="hero-video" autoplay muted loop>
      <source src={heroVideo} type="video/mp4" />
      Your browser does not support the video tag.
    </video>
    <div class="hero-overlay"></div>
  </div>

  <div class="hero-content">
    <div class="hero-logo-title">
      <h1 class="company-title">
        <span class="accentfont">K</span>ahlenberg & <span class="accentfont">C</span>hristiansen
      </h1>
      <img src={logo.src} alt="Kahlenberg & Christiansen Logo" class="hero-logo" />
    </div>
    <div class="hero-text">
      <h2 class="hero-heading">Udvalgte vine med karakter - direkte til jeres vinliste.</h2>
      <p class="hero-subtext">Vi importerer nøje udvalgte vine fra passionerede vinbønder i Europa og videreformidler dem til restauranter, der værdsætter håndværk, terroir og personlighed i glasset.</p>
    </div>
  </div>
</header>

<style>
  .hero-header {
    position: relative;
    height: 100%;
    overflow: hidden;
    color: white;
  }

  .hero-video-container {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
  }

  .hero-video {
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: brightness(0.8);
  }

  .hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(to bottom, rgba(65, 20, 21, 0) 0%, rgba(65, 20, 21, 1) 100%);
    z-index: 2;
  }

  .hero-content {
    position: relative;
    z-index: 3;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    gap: 20vh;
    height: 100%;
    text-align: center;
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem;
  }

  .hero-logo-title {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .hero-logo {
    width: 150px;
    height: auto;
    margin-bottom: 0.5rem;
    transition: ease 0.3s;
  }

  .hero-text {
    text-align: left;
    max-width: 600px;
    width: 80%;
    margin: 0;
    transition: ease 0.3s;
  }

  .company-title {
    font-size: 1.5rem;
    min-width: 100%;
    white-space: nowrap;
  }

  .accentfont {
    font-size: 3rem;
  }

  .hero-heading {
    margin-bottom: 1rem;
  }

  .hero-subtext {
    line-height: 1.6;
    color: #d1d5db;
  }

  @media (min-width: 768px) {
    .hero-content {
      min-height: 90vh;
      margin-bottom: 100px;
    }
    .hero-logo {
      width: 300px;
      height: auto;
      margin-bottom: 0.5rem;
      transition: ease 0.3s;
    }

    .company-title {
      font-size: 3rem;
    }

    .accentfont {
      font-size: 5rem;
    }
  }
</style>
