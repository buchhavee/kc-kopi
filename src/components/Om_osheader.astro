---
import Navbar from "./Navbar.astro";
---

<header class="hero-header">
  <nav class="navbar">
    <Navbar />
  </nav>
  <div class="overlay">
    <div class="redoverlay">
      <div class="headergrid container">
        <div class="headercontent">
          <div class="hero-text">
            <h2>Vores rejse til at finde de bedste vine</h2>
            <p>Grundlagt på en passion for dejlige vine. <br /> Vi stolte af processen bag indkøbet af vores fine vin fra velkendte vingårde, med en enestående historie bag. Vi er dedikeret til at tilbyde vin af højeste kvalitet og at sikre os at vores vinimport og oplevelser er af de højeste standartarter.</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</header>

<style>
  header {
    background-image: url("/src/assets/omoshero.webp");
    height: 70vh;
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    position: relative;
  }
  .overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.3);
    z-index: 100;
  }

  .hero-text {
    max-width: 600px;
  }
  .container {
    max-width: 1200px;
    margin-inline: auto;
    padding: 2rem;
    box-sizing: border-box;
  }
  .redoverlay {
    background: linear-gradient(180deg, rgba(65, 20, 21, 0) 0%, rgba(65, 20, 21, 1) 100%);
    width: 100%;
    height: 100%;
    grid-row: 1 / 2;
    grid-column: 1 / 2;
    z-index: 1;
  }
  .headergrid {
    display: grid;
    grid-template-columns: 1fr;
    grid-template-rows: 1fr;
    position: relative;
    max-height: 100vh;
    overflow: hidden;
    height: 100%;
  }
  .navbar {
    grid-column: 1 / 2;
    grid-row: 1 / 2;
  }
  /* Removed img styles */
  .headercontent {
    position: absolute;
    bottom: 30px;
    color: white;
    z-index: 2;
    padding: 2rem;
    left: 0;
    right: 0;
  }
</style>
